namespace DefaultNamespace
{
    public enum MessageEnum
    {
        SERVER_SHUTDOWN,
        DISCONNECT,
        CREATE_ROOM,
        JOIN_ROOM,
        SEND_TO_ROOM,
        DELETE_ROOM,
        CLIENT_LIST,
        REMOVE_PARTICIPANT
    }
}